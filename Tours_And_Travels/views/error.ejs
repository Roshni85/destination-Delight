
    <title>Error</title>
    <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 80vh;
            text-align: center;
            flex-direction: column;
        }
        .error-container {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            width: 90%;
            margin-top: 20px;
        }
        .error-title {
            color: #dc3545;
            font-weight: bold;
            font-size: 28px;
        }
        .error-message {
            color: #6c757d;
            font-size: 18px;
            margin-top: 10px;
        }
        .back-button {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <!-- Navbar will remain at the top (from layout.ejs) -->

    <!-- Error Message Container -->
    <div class="error-container">
        <h1 class="error-title">Oops! Something went wrong.</h1>
        <p class="error-message"><%= error.message %></p>
        <a href="/packages" class="btn btn-primary back-button">Go Back Home</a>
    </div>
</body>
</html>
